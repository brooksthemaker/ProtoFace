"""Standalone face editor for Protoface (pygame). Run via ``python editor.py``.

Edit-only: this package is never imported by the render daemon (run.py). It
reads and writes the same faces/<name>/ folders (PNGs + config.json) that the
renderer consumes, so what you draw here is exactly what the panels show.
"""
