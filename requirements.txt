numpy>=1.24
Pillow>=10.0
PyYAML>=6.0
pygame>=2.5        # preview window + face editor (dev only)
pyaudio>=0.2.13    # microphone input (optional)
pyserial>=3.5      # button coprocessor input (optional)
# On CM5 / Raspberry Pi 5, also install:
#   pip install Adafruit-Blinka-Raspberry-Pi5-Piomatter  (HUB75 via RP1 PIO; needs /dev/pio0)
#   sudo apt install python3-smbus                        (I2C for gyro)
#   pip install RPi.GPIO                                  (boop sensor)
# NOTE: hzeller's rgbmatrix does NOT work on CM5/Pi 5 — use Piomatter (above).
# On Pi Zero 2W / Pi 3 / Pi 4 (classic RGB Matrix Bonnet), build hzeller's
# rgbmatrix python binding instead — see PI02W.md (Piomatter does NOT work
# there: no RP1, no /dev/pio0).
